# Enable the configuration cache
org.gradle.configuration-cache=true

# Tell Gradle to fail the build if a plugin or task is incompatible 
org.gradle.configuration-cache.inputs.strict=true

# Optional: Parallelize task execution for faster multi-project builds
org.gradle.parallel=true